subscription OnGamestateCreated($saveId: Int!) {
  gamestateCreated(saveId: $saveId) {
    gamestateId
    date
    budget {
      balance {
        countryBase {
          energy
          minerals
          food
          trade
          alloys
          consumerGoods
          unity
          influence
        }
      }
    }
  }
}
